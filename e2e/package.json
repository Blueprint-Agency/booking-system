{
  "name": "reservetoday-e2e",
  "private": true,
  "description": "Browser journeys — on the local stack for every PR (#207), against staging to gate production (#145)",
  "scripts": {
    "test": "playwright test",
    "check": "tsc --noEmit",
    "agents": "node scripts/init-agents.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
