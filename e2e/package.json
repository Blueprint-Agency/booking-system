{
  "name": "reservetoday-e2e",
  "private": true,
  "description": "Browser journeys against a deployed ReserveToday — staging gates production (#145)",
  "scripts": {
    "test": "playwright test",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
