# Journey plans

The Playwright planner agent's Markdown plans, one area per file (`member-waitlist.md`,
`admin-refunds.md`, …). A human reviews a plan before the generator turns it into
journeys in `../journeys/`. How: `docs/md/e2e-journeys.md`.
