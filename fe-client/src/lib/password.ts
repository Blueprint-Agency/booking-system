/**
 * The shortest member password the backend accepts (#173). Mirrored here, not
 * imported — the apps share no code — so the forms can say so before a round trip.
 */
export const MIN_PASSWORD_LENGTH = 8;
